import { DarkOnHouverDirective } from './dark-on-houver.directive';

describe('DarkOnHouverDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkOnHouverDirective();
    expect(directive).toBeTruthy();
  });
});
